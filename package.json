{
  "name": "gwgc2018",
  "version": "1.0.0",
  "devDependencies": {
    "terser": "^3.14.0"
  },
  "scripts": {
    "build": "terser app.js -o app-min.js --config-file terser.config.json",
    "get-sha-windows": "Get-FileHash -Algorithm SHA256 grand-hackster.zip"
  }
}
